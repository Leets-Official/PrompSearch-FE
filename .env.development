# 개발(next dev) 전용 환경 변수. BE API 확정 전까지 MSW 목을 사용한다.
# BE 실서버가 준비되면 이 값을 지우거나 disabled 로 바꾼다.
NEXT_PUBLIC_API_MOCKING=enabled

# 헤더 로그인 상태(알림+프로필) 미리보기용 토글. 실제 auth 소스가 없는 동안만 사용.
# authenticated 로 켜면 회원 헤더가 보인다(주석 처리 시 비회원). 실제 인증 붙으면 제거.
# NEXT_PUBLIC_MOCK_AUTH=authenticated
